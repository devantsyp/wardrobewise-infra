---
phase: 03-care-label-analysis-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - wardrobe/views.py
  - wardrobe/urls.py
  - templates/wardrobe/garment_detail.html
  - templates/wardrobe/wardrobe_list.html
  - templates/base.html
autonomous: true

must_haves:
  truths:
    - "User clicks 'Analyze Care Label' on garment detail page and receives structured care instructions"
    - "Analyze button is disabled with tooltip when no care label photo exists"
    - "Loading state shows spinner + 'Analyzing...' text during synchronous POST"
    - "Results display as summary sentence followed by bullet list"
    - "Warnings and prohibitions are visually distinct"
    - "Delicates flag shows as a warning badge when true"
    - "Care label thumbnail shown alongside instructions"
    - "Timestamp shown as 'Analyzed X days ago' with dedup note when from_cache"
    - "Pre-analysis placeholder shows when no analysis exists"
    - "Rate limit message replaces Analyze button when daily limit reached with countdown to midnight"
    - "Budget guard message replaces Analyze button when $9 threshold hit"
    - "Daily counter 'X / 10 today' visible in nav on wardrobe pages with color coding"
    - "Analyzed garments show a visual indicator badge on wardrobe grid"
    - "Analysis failure shows inline error with Retry button"
  artifacts:
    - path: "wardrobe/views.py"
      provides: "analyze_care_label_view POST handler"
      contains: "def analyze_care_label_view"
    - path: "wardrobe/urls.py"
      provides: "URL route for analyze endpoint"
      contains: "analyze"
    - path: "templates/wardrobe/garment_detail.html"
      provides: "Care instructions display, analyze button, rate limit UI, edit/re-analyze buttons"
      contains: "Analyze Care Label"
    - path: "templates/wardrobe/wardrobe_list.html"
      provides: "Analysis badge on garment cards"
      contains: "care_analysis"
    - path: "templates/base.html"
      provides: "Daily usage counter in nav"
      contains: "daily_usage_count"
  key_links:
    - from: "wardrobe/views.py"
      to: "wardrobe/services/analysis.py"
      via: "calls analyze_care_label() and catches exceptions"
      pattern: "from wardrobe.services.analysis import"
    - from: "templates/wardrobe/garment_detail.html"
      to: "wardrobe/views.py"
      via: "POST form targets analyze URL"
      pattern: "wardrobe:analyze_care_label"
    - from: "templates/base.html"
      to: "wardrobe/context_processors.py"
      via: "uses daily_usage_count and daily_usage_limit template variables"
      pattern: "daily_usage_count"
---

<objective>
Build the analyze view, garment detail template with full care instructions display (results, rate limit UI, loading state, error handling), nav counter, and wardrobe grid badge.

Purpose: This is the core user-facing analysis flow — the primary feature of Phase 3. After this plan, users can trigger analysis, see results, and understand their rate limit status.

Output: Working analyze endpoint, updated garment detail page with instructions section, nav counter on wardrobe pages, analysis badge on grid cards.
</objective>

<execution_context>
@C:/Users/patel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-care-label-analysis-pipeline/03-CONTEXT.md
@.planning/phases/03-care-label-analysis-pipeline/03-RESEARCH.md
@.planning/phases/03-care-label-analysis-pipeline/03-01-SUMMARY.md
@wardrobe/views.py
@wardrobe/urls.py
@wardrobe/models.py
@templates/wardrobe/garment_detail.html
@templates/wardrobe/wardrobe_list.html
@templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze view + URL route + garment detail view update</name>
  <files>
    wardrobe/views.py
    wardrobe/urls.py
  </files>
  <action>
**Update wardrobe/views.py:**

1. Add imports at top:
   - `from django.contrib import messages`
   - `from wardrobe.services.analysis import analyze_care_label, RateLimitExceeded, BudgetGuardTripped, AnalysisError`

2. **Update `garment_detail` view:** Use `select_related` or `prefetch_related` is not needed since OneToOneField access is automatic. The existing view already passes the garment to the template — the template can access `garment.care_analysis` directly via the reverse OneToOne relation. No changes needed to the garment_detail view function — it already renders the garment correctly and the template will handle conditional display using `garment.care_analysis`.

   However, add a check for Django messages to handle the flash message for midnight reset. The messages framework is already available via context processor — no view change needed.

3. **Create `analyze_care_label_view`:**
   ```python
   @login_required
   @require_POST
   def analyze_care_label_view(request, pk):
       garment = get_object_or_404(Garment, pk=pk, user=request.user)

       if not garment.care_label_photo:
           messages.error(request, "Please upload a care label photo first.")
           return redirect('wardrobe:garment_detail', pk=pk)

       try:
           analyze_care_label(garment, request.user)
           messages.success(request, "Care instructions analyzed successfully.")
       except RateLimitExceeded:
           messages.error(request, "daily_limit_reached")
       except BudgetGuardTripped:
           messages.error(request, "budget_guard_tripped")
       except AnalysisError as e:
           messages.error(request, f"analysis_failed")
       return redirect('wardrobe:garment_detail', pk=pk)
   ```

   Note: The error message strings ("daily_limit_reached", "budget_guard_tripped", "analysis_failed") are sentinel values — the template checks for these specific strings to render appropriate UI. The actual user-facing messages are in the template.

**Update wardrobe/urls.py:**

Add URL pattern: `path('<int:pk>/analyze/', views.analyze_care_label_view, name='analyze_care_label'),`
  </action>
  <verify>
Run: `python -c "import django; django.setup(); from wardrobe.views import analyze_care_label_view; print('View imported')"` (with DJANGO_SETTINGS_MODULE=laundry_advisor.settings.dev).
Run: `grep 'analyze_care_label' wardrobe/urls.py` — should show the URL pattern.
  </verify>
  <done>analyze_care_label_view exists as a POST-only endpoint that calls the service layer, catches all three exception types, and redirects to garment detail with appropriate messages. URL route registered at /wardrobe/{pk}/analyze/.</done>
</task>

<task type="auto">
  <name>Task 2: Garment detail template — care instructions display, analyze button, rate limit UI</name>
  <files>
    templates/wardrobe/garment_detail.html
    templates/base.html
    templates/wardrobe/wardrobe_list.html
  </files>
  <action>
**CRITICAL: Read all three template files before editing. Preserve existing structure and styling patterns.**

**1. Update `templates/wardrobe/garment_detail.html`:**

Replace the existing care instructions placeholder (`<div class="bg-deep-space-50 rounded-lg p-4 mb-6">..Care instructions coming soon..</div>`) with a full care instructions section. This section sits BELOW the garment fields (below color/fabric/notes) and ABOVE the action buttons, per locked decisions.

The section must handle these states:

**State A — No analysis exists AND no care label photo:**
```html
<div class="bg-deep-space-50 rounded-lg p-4 mb-6">
  <p class="text-thistle-500 italic text-sm">
    No care instructions yet. Upload a care label photo and click Analyze to get started.
  </p>
</div>
```
Also show the disabled Analyze button with tooltip:
```html
<button disabled
  title="Upload a care label photo first to enable analysis"
  class="bg-deep-space-200 text-deep-space-400 font-semibold px-6 py-3 rounded-lg cursor-not-allowed">
  Analyze Care Label
</button>
```

**State B — No analysis exists AND care label photo exists AND NOT rate limited AND NOT budget guarded:**
Show the placeholder text AND the active Analyze button:
```html
<form method="POST" action="{% url 'wardrobe:analyze_care_label' garment.pk %}" id="analyze-form">
  {% csrf_token %}
  <button type="submit" id="analyze-btn"
    class="bg-dark-teal-600 hover:bg-dark-teal-700 text-white font-semibold px-6 py-3 rounded-lg transition-colors">
    Analyze Care Label
  </button>
</form>
```
Add JS for loading state — on form submit, disable button and change text to spinner + "Analyzing...":
```javascript
document.getElementById('analyze-form').addEventListener('submit', function() {
  var btn = document.getElementById('analyze-btn');
  btn.disabled = true;
  btn.innerHTML = '<svg class="animate-spin inline-block w-4 h-4 mr-2" ...spinner SVG.../> Analyzing...';
  btn.classList.remove('hover:bg-dark-teal-700');
  btn.classList.add('bg-deep-space-400', 'cursor-wait');
});
```
Use a simple inline SVG spinner (a rotating circle with a gap — standard Tailwind spinner pattern using `animate-spin`).

**State C — Rate limit reached (daily_usage_count >= daily_usage_limit):**
Hide the Analyze button. Show inline block:
```html
<div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6">
  <p class="text-amber-800 font-medium">
    Daily limit reached ({{ daily_usage_count }} / {{ daily_usage_limit }}).
    Resets in <span id="midnight-countdown" class="font-mono"></span>
  </p>
</div>
```
Include the vanilla JS countdown timer (from research Pattern 7):
```javascript
(function() {
  function getMsUntilMidnight() {
    var now = new Date();
    var midnight = new Date(now);
    midnight.setHours(24, 0, 0, 0);
    return midnight - now;
  }
  function formatCountdown(ms) {
    var totalSeconds = Math.floor(ms / 1000);
    var h = Math.floor(totalSeconds / 3600);
    var m = Math.floor((totalSeconds % 3600) / 60);
    var s = totalSeconds % 60;
    return h + 'h ' + m + 'm ' + s + 's';
  }
  function tick() {
    var el = document.getElementById('midnight-countdown');
    if (!el) return;
    el.textContent = formatCountdown(getMsUntilMidnight());
    setTimeout(tick, 1000);
  }
  tick();
})();
```

**State D — Budget guard tripped:**
```html
<div class="bg-deep-space-50 rounded-lg p-4 mb-6">
  <p class="text-deep-space-500 font-medium">Analysis temporarily unavailable.</p>
</div>
```

**State E — Analysis exists (garment.care_analysis):**
Show the full results section:

a. **Summary sentence** — `{{ garment.care_analysis.summary }}` in bold/medium weight text.

b. **Bullet list** of structured fields:
   ```html
   <ul class="list-disc list-inside space-y-2 text-deep-space-800 mt-3">
     <li><strong>Washing:</strong> {{ garment.care_analysis.washing }}</li>
     <li><strong>Drying:</strong> {{ garment.care_analysis.drying }}</li>
     <li><strong>Ironing:</strong> {{ garment.care_analysis.ironing }}</li>
     <li class="{% if 'not' in garment.care_analysis.bleach|lower or 'no' in garment.care_analysis.bleach|lower %}text-red-700 font-medium{% endif %}">
       <strong>Bleach:</strong> {{ garment.care_analysis.bleach }}
     </li>
   </ul>
   ```
   Style bleach warnings (containing "not" or "no") with red text for visual distinction per locked decision. Also style any bullet containing prohibition keywords.

c. **Delicates badge** — when `garment.care_analysis.is_delicate` is True:
   ```html
   <span class="inline-flex items-center gap-1 bg-amber-100 text-amber-800 text-xs font-semibold px-2.5 py-1 rounded-full mt-3">
     ⚠ Delicate Item
   </span>
   ```
   Note: This is an exception to the no-emoji rule — the warning symbol is functional UI, per the locked decision for "warning icon." Use the HTML entity `&#9888;` or the text "!" in a styled circle instead of the emoji if preferred, but a simple warning indicator is needed.

d. **Personal notes** — if `garment.care_analysis.personal_notes`:
   ```html
   <p class="text-deep-space-600 text-sm mt-3 italic">
     <strong>Notes:</strong> {{ garment.care_analysis.personal_notes }}
   </p>
   ```

e. **Care label thumbnail** — small image alongside instructions for cross-reference:
   ```html
   {% if garment.care_label_photo %}
   <img src="{{ garment.care_label_photo.url }}" alt="Care label"
     class="w-20 h-auto rounded mt-3 border border-deep-space-100">
   {% endif %}
   ```

f. **Timestamp** with dedup note:
   ```html
   <p class="text-thistle-400 text-xs mt-3">
     Analyzed {{ garment.care_analysis.analyzed_at|timesince }} ago
     {% if garment.care_analysis.from_cache %}
     <span class="text-thistle-300"> — (from previous analysis)</span>
     {% endif %}
   </p>
   ```

g. **Action buttons** below the instructions:
   - "Edit Instructions" button (links to edit URL — will be wired in Plan 03-03)
   - "Re-analyze" button (POST form to analyze URL). If `garment.care_analysis.is_user_edited`, add JS confirm: "Re-analyzing will replace your edited instructions. Continue?"
   ```html
   <div class="flex items-center gap-3 mt-4">
     <a href="{% url 'wardrobe:edit_instructions' garment.pk %}"
       class="bg-deep-space-100 hover:bg-deep-space-200 text-deep-space-800 font-semibold px-4 py-2 rounded-lg transition-colors text-sm">
       Edit Instructions
     </a>
     <form method="POST" action="{% url 'wardrobe:analyze_care_label' garment.pk %}"
       {% if garment.care_analysis.is_user_edited %}
       onsubmit="return confirm('Re-analyzing will replace your edited instructions. Continue?');"
       {% endif %}>
       {% csrf_token %}
       <button type="submit"
         class="bg-deep-space-100 hover:bg-deep-space-200 text-deep-space-800 font-semibold px-4 py-2 rounded-lg transition-colors text-sm">
         Re-analyze
       </button>
     </form>
   </div>
   ```
   NOTE: The `edit_instructions` URL will not exist yet (it's in Plan 03-03). Use the URL name `wardrobe:edit_instructions` — Plan 03-03 will create the matching URL pattern and view. For now, to avoid a NoReverseMatch error during this plan's verification, wrap the Edit Instructions link in `{% url 'wardrobe:edit_instructions' garment.pk %}` BUT ALSO add the URL route stub in wardrobe/urls.py in Task 1 (add a placeholder path that returns 404 or redirects, to be replaced in Plan 03-03). ALTERNATIVELY, use a simple `#` href placeholder and add a comment `{# TODO: Plan 03-03 will wire this to wardrobe:edit_instructions #}` — this approach is simpler and avoids fake URL routes.

   **Decision: Use href="#" placeholder** for Edit Instructions link with a TODO comment. Plan 03-03 will replace it.

h. **Error state** — if Django messages contain "analysis_failed":
   ```html
   {% for message in messages %}
     {% if 'analysis_failed' in message.message %}
     <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
       <p class="text-red-800 font-medium">Analysis failed. Please try again.</p>
       <form method="POST" action="{% url 'wardrobe:analyze_care_label' garment.pk %}" class="mt-2">
         {% csrf_token %}
         <button type="submit" class="bg-red-600 hover:bg-red-700 text-white font-semibold px-4 py-2 rounded-lg text-sm">
           Retry
         </button>
       </form>
     </div>
     {% endif %}
   {% endfor %}
   ```

**Template logic order (use Django template conditionals):**
1. Check for error messages first (show error + retry if present)
2. Check `budget_guard_tripped` — if True, show budget message
3. Check `daily_usage_count >= daily_usage_limit` — if True, show rate limit message with countdown
4. Check if `garment.care_analysis` exists (use `{% with analysis=garment.care_analysis %}{% if analysis %}...{% endif %}{% endwith %}` or try/except approach — in Django templates, accessing a non-existent OneToOne raises DoesNotExist which templates handle by treating as falsy, so `{% if garment.care_analysis %}` may not work. Instead, use a pattern like checking a variable set in the view, OR use `{% for analysis in garment.care_analysis_set %}` — but that won't work with OneToOneField. The safest approach: in the garment_detail view, add `has_analysis = hasattr(garment, 'care_analysis') and garment.care_analysis is not None` as a try/except block, and pass it as context. OR simpler: use `{% if garment.care_analysis.pk %}` which will be falsy if no analysis exists (accessing .pk on a non-existent reverse OneToOne in Django templates returns empty string, which is falsy).

   **Decision:** Update the `garment_detail` view to pass `analysis` as explicit context:
   ```python
   try:
       analysis = garment.care_analysis
   except CareAnalysis.DoesNotExist:
       analysis = None
   context = {'garment': garment, 'analysis': analysis}
   ```
   This requires importing CareAnalysis in views.py. Then the template uses `{% if analysis %}`.

5. If no analysis, show placeholder + Analyze button (enabled or disabled based on care_label_photo)

**2. Update `templates/base.html`:**

Add the daily usage counter to the nav bar. Per locked decision: visible only on wardrobe pages (context processor handles this — variables only exist on wardrobe namespace pages). Display format: "X / 10 today".

Inside the `<nav>` element, after the existing nav content, add:
```html
{% if daily_usage_count is not None %}
<div class="flex items-center gap-2 text-sm">
  <span class="{% if daily_usage_count >= daily_usage_limit %}text-red-600 font-bold{% elif daily_usage_count >= 7 %}text-amber-600 font-medium{% else %}text-deep-space-500{% endif %}">
    {{ daily_usage_count }} / {{ daily_usage_limit }} today
  </span>
</div>
{% endif %}
```

Color thresholds (Claude's discretion): 0-6 = normal (deep-space-500), 7-9 = warning (amber-600), 10 = red (red-600 bold).

Read the full base.html first to find the correct insertion point within the nav structure.

**3. Update `templates/wardrobe/wardrobe_list.html`:**

Add an analysis badge to garment cards in the grid. Per locked decision: "analyzed garments show a visual indicator badge."

On each garment card in the grid loop, after the garment name or on the card image area, add:
```html
{% if garment.care_analysis %}
<span class="absolute top-2 right-2 bg-dark-teal-600 text-white text-xs font-bold w-5 h-5 rounded-full flex items-center justify-center" title="Care label analyzed">
  ✓
</span>
{% endif %}
```

Note: This requires the card's image container to have `position: relative`. Check the existing template — if the container doesn't have `relative`, add it.

**IMPORTANT:** The `{% if garment.care_analysis %}` check in templates for a OneToOneField reverse relation may cause an extra query per garment (N+1). To optimize, update the `garment_list` view to use `select_related('care_analysis')` — BUT `select_related` doesn't work for reverse OneToOne (it works for the forward direction). Use `prefetch_related('care_analysis')` instead in the garment_list queryset in views.py. Add this optimization to the garment_list view:
```python
garments = Garment.objects.filter(user=request.user).prefetch_related('care_analysis')
```

However, accessing a reverse OneToOne in a template with `{% if garment.care_analysis %}` still raises DoesNotExist for garments without analysis. Use the safer pattern:
```html
{% for garment in garments %}
  {# Badge check: use hasattr-like template filter or annotate #}
{% endfor %}
```

**Simplest solution:** In the `garment_list` view, annotate with `has_analysis`:
```python
from django.db.models import Exists, OuterRef
from wardrobe.models import CareAnalysis

garments = Garment.objects.filter(user=request.user).annotate(
    has_analysis=Exists(CareAnalysis.objects.filter(garment=OuterRef('pk')))
)
```
Then in the template: `{% if garment.has_analysis %}...badge...{% endif %}`. This is a single query with a subquery — no N+1.
  </action>
  <verify>
1. Run `python manage.py check` — no template or system errors.
2. Start dev server (`python manage.py runserver`) and manually verify:
   - Visit garment detail page for a garment WITHOUT care label — see disabled Analyze button with tooltip and placeholder text.
   - Visit garment detail page for a garment WITH care label — see enabled Analyze button and placeholder text.
   - Check nav bar on wardrobe pages — see "0 / 10 today" counter.
   - Check nav bar on non-wardrobe pages (e.g., admin) — counter NOT shown.
   - Visit wardrobe grid — no analysis badges (none analyzed yet).
3. Run `grep -c "analyze" templates/wardrobe/garment_detail.html` — confirms analyze-related content present.
  </verify>
  <done>Garment detail page shows care instructions section with all states (placeholder, results, rate limit, budget guard, error). Analyze button triggers synchronous POST with loading state. Nav counter shows daily usage on wardrobe pages. Wardrobe grid shows analysis badge. All per locked decisions.</done>
</task>

</tasks>

<verification>
1. `python manage.py check` — no errors
2. Garment detail page shows correct state for: no care label, care label but no analysis, analysis exists
3. Analyze button triggers POST to correct URL and redirects back to detail
4. Nav counter appears on wardrobe pages only
5. Rate limit and budget guard messages render correctly (can be verified by template inspection since triggering requires API calls)
6. Wardrobe grid annotates has_analysis without N+1 queries
</verification>

<success_criteria>
- POST /wardrobe/{pk}/analyze/ endpoint calls analyze_care_label() service and redirects to detail page
- Garment detail page handles 5 states: no photo, pre-analysis, results displayed, rate limited, budget guarded
- Results show as summary + bullet list with warning styling per locked decisions
- Delicates badge, care label thumbnail, timestamp with dedup note all present
- Loading state shows spinner + "Analyzing..." on button click
- Midnight countdown timer works in rate-limit-reached state
- Nav counter is color-coded (normal/warning/red) and only appears on wardrobe pages
- Wardrobe grid cards show analysis badge for analyzed garments
</success_criteria>

<output>
After completion, create `.planning/phases/03-care-label-analysis-pipeline/03-02-SUMMARY.md`
</output>
