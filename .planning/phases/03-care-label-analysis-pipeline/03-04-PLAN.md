---
phase: 03-care-label-analysis-pipeline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - wardrobe/views.py
  - templates/wardrobe/garment_detail.html
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "When the daily rate limit is hit, the user sees 'Daily analysis limit reached. Try again tomorrow.' — not a raw token"
    - "When the global budget guard is tripped, the user sees 'Analysis temporarily unavailable.' — not a raw token"
    - "The analysis_failed error does not appear as a generic base toast; it is only rendered inline in garment_detail.html with a Retry button"
  artifacts:
    - path: "wardrobe/views.py"
      provides: "Human-readable error messages for all three exception paths"
      contains: "Daily analysis limit reached"
    - path: "templates/wardrobe/garment_detail.html"
      provides: "Inline analysis_failed error block filtered out of base toast"
  key_links:
    - from: "wardrobe/views.py"
      to: "base.html toast renderer"
      via: "messages.error()"
      pattern: "messages\\.error.*Daily analysis"
    - from: "templates/wardrobe/garment_detail.html"
      to: "base.html messages loop"
      via: "extra_tags filter"
      pattern: "extra_tags.*analysis_failed"
---

<objective>
Fix raw internal token strings passed to messages.error() that appear verbatim as red error toasts in the base template.

Purpose: Truth 3 of the phase goal requires that users see human-readable feedback when the rate limit or budget guard blocks an analysis. Raw tokens like "daily_limit_reached" violate this and represent a broken user-facing error state.
Output: Three message strings replaced with readable copy; the analysis_failed toast suppressed from base.html since garment_detail.html renders it inline with a Retry button.
</objective>

<execution_context>
@C:/Users/vlada/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vlada/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-care-label-analysis-pipeline/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw error tokens with human-readable messages</name>
  <files>wardrobe/views.py</files>
  <action>
    In wardrobe/views.py, replace the three raw token strings inside the analyze_care_label_view exception handlers (currently at lines 98, 100, 102):

    - Line 98: `messages.error(request, "daily_limit_reached")`
      Replace with: `messages.error(request, "Daily analysis limit reached. Try again tomorrow.", extra_tags="daily_limit_reached")`

    - Line 100: `messages.error(request, "budget_guard_tripped")`
      Replace with: `messages.error(request, "Analysis temporarily unavailable.", extra_tags="budget_guard_tripped")`

    - Line 102: `messages.error(request, "analysis_failed")`
      Replace with: `messages.error(request, "Analysis failed. Please try again.", extra_tags="analysis_failed")`

    The extra_tags field preserves the token as a CSS/logic hook so garment_detail.html can still match on it (currently checks `'analysis_failed' in message.message` — update that check too, see Task 2). The human-readable text is what {{ message }} will now render in the base toast.

    Per CONTEXT.md locked decisions:
    - Budget guard copy: "Analysis temporarily unavailable." (exact match)
    - Daily limit copy: uses "Daily analysis limit reached. Try again tomorrow." (inline block on garment_detail handles the countdown separately; this toast is only shown on non-detail-page redirects, but must be readable regardless)
  </action>
  <verify>
    grep -n "messages.error" wardrobe/views.py
    Confirm lines 98, 100, 102 now contain human-readable strings and extra_tags= arguments.
  </verify>
  <done>
    All three messages.error() calls in the exception block contain plain English copy. No raw token strings ("daily_limit_reached", "budget_guard_tripped", "analysis_failed") appear as the first positional argument to messages.error().
  </done>
</task>

<task type="auto">
  <name>Task 2: Suppress analysis_failed from base toast and fix garment_detail match</name>
  <files>templates/wardrobe/garment_detail.html</files>
  <action>
    In templates/wardrobe/garment_detail.html, the inline error block (around line 216) currently matches on `'analysis_failed' in message.message`. Since Task 1 moves the token to extra_tags, update the match to use extra_tags instead:

    Change:
      `{% if 'analysis_failed' in message.message %}`
    To:
      `{% if 'analysis_failed' in message.extra_tags %}`

    This preserves the inline Retry block on the garment detail page.

    Then, in templates/base.html, add a filter inside the messages for-loop to suppress any message whose extra_tags contains "analysis_failed" from the generic toast renderer:

    Find the existing toast for-loop (around line 45):
      `{% for message in messages %}`
    Add a guard immediately inside:
      `{% if 'analysis_failed' not in message.extra_tags %}`
    And close it before the `{% endfor %}`:
      `{% endif %}`

    This ensures the analysis_failed message is consumed only by the garment_detail inline block, never by the base toast. On any other page (where garment_detail template is not active), the message silently disappears — acceptable because analysis always redirects back to the garment detail page.
  </action>
  <verify>
    grep -n "extra_tags" templates/wardrobe/garment_detail.html
    grep -n "analysis_failed" templates/base.html
    Confirm garment_detail uses extra_tags for match, and base.html excludes analysis_failed from toast.
  </verify>
  <done>
    garment_detail.html matches analysis_failed via extra_tags. base.html toast loop skips messages with extra_tags containing "analysis_failed". The inline Retry block still renders on the garment detail page.
  </done>
</task>

</tasks>

<verification>
Manual smoke-test (dev server):
1. Trigger a rate-limit scenario (or temporarily lower the daily limit to 0 in settings) — the red toast at top-right should read "Daily analysis limit reached. Try again tomorrow." — not "daily_limit_reached".
2. Trigger a budget guard scenario (or mock BudgetGuardTripped) — toast should read "Analysis temporarily unavailable." — not "budget_guard_tripped".
3. Trigger an analysis failure — garment_detail inline block should show "Analysis failed. Please try again." with Retry button; NO duplicate toast should appear in the top-right corner.

Code check (no server needed):
  grep -n "daily_limit_reached\|budget_guard_tripped\|analysis_failed" wardrobe/views.py
  — must return zero lines where the token is the message body (extra_tags= occurrences are fine).
</verification>

<success_criteria>
- wardrobe/views.py exception handlers contain plain-English copy in the message body for all three error paths
- base.html toast loop skips analysis_failed messages
- garment_detail.html inline error block matches via extra_tags, not message body
- No raw token string is ever rendered directly to the user in any template
</success_criteria>

<output>
After completion, create `.planning/phases/03-care-label-analysis-pipeline/03-04-SUMMARY.md`
</output>
